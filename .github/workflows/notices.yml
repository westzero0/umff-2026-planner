name: Update UMFF notices

on:
  schedule:
    - cron: '0 22 * * *'  # 07:00 KST daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/fetch-notices.js
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add notices.json
          git diff --staged --quiet || git commit -m "Update UMFF notices"
          git push
